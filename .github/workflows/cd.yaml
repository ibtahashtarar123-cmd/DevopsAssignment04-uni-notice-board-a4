name: cd-wf
on: 
  push:
    branches:
      - develop
jobs:
  deploy-dev-job:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: Deploy Website
        run: curl -X POST ${{ secrets.RENDER_CALLBACK_URL}}